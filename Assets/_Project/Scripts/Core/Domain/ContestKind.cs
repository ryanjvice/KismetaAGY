namespace Kismeta.Core.Domain
{
    /// <summary>Player-vs-player contest types that use initiate → respond → resolve flow.</summary>
    public enum ContestKind
    {
        Trade,
        Duel,
        Gambit,
        Opposition
    }
}
